name: Test

on:
  workflow_dispatch:  
  push:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: macOS-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: File hierarchy
        run: ls -R
      - name: Check pod version
        run: pod --version
        shell: bash
    
